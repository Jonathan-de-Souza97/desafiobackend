services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: 76872985
      POSTGRES_DB: app
    ports:
      - "5432:5432"
    volumes:
      - ./scriptSql/create.pgsql:/docker-entrypoint-initdb.d/create.sql